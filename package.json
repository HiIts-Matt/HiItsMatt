{
  "name": "hiitsmatt",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "node scripts/dev.js",
    "dev:raw": "concurrently --kill-others-on-fail --names api,web --prefix-colors blue,magenta \"npm -w server run dev\" \"npm -w client run dev\"",
    "build": "npm -w server run build && npm -w client run build",
    "start": "npm -w server run start",
    "deploy": "node scripts/deploy.js",
    "typecheck": "npm -w server run typecheck && npm -w client run typecheck"
  },
  "devDependencies": {
    "@clack/prompts": "^1.7.0",
    "concurrently": "^10.0.5",
    "terminal-kit": "^3.1.3"
  },
  "engines": {
    "node": ">=22.12"
  }
}
