{
  "name": "hiitsmatt",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently --names api,web --prefix-colors blue,magenta \"npm -w server run dev\" \"npm -w client run dev\"",
    "build": "npm -w server run build && npm -w client run build",
    "start": "npm -w server run start",
    "typecheck": "npm -w server run typecheck && npm -w client run typecheck"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  },
  "engines": {
    "node": ">=22.12"
  }
}
